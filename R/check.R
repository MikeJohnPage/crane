#' @keywords internal
check_package <- function() {
  # Check that a package structure doesn't already exist
}

#' @keywords internal
check_path_exists <- function() {
  # Soft wrapper to file/dir.exists()
}