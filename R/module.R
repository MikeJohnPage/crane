module_plotly <- function() {
  # Sensible default: scatterplot + styling + tests
}

module_leaflet <- function() {
  # Sensible default: basemaps + tests
}

module_select <- function() {
  # Sensible default: holding options + tests
}